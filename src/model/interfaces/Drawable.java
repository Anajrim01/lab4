package model.interfaces;

public interface Drawable {
    // interface to get the x and y coordinates of a drawable object and to link them
    double getX();

    double getY();

    void setX(double x);

    void setY(double y);
}
